<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@drawable/new_background_fat"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/loginImageBackground"
        android:layout_width="250dp"
        android:layout_height="200dp"
        android:layout_alignParentTop="true"
        android:layout_marginTop="-30dp"
        android:layout_centerHorizontal="true"
        android:background="@null"
        android:src="@drawable/bg_logo" />

    <TextView
        android:id="@+id/i_am_hungry"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/loginImageBackground"
        android:layout_centerHorizontal="true"
        android:text="@string/i_am_hungry_eat_planet"
        android:textAppearance="@style/FontSegoeUI"
        android:textSize="25sp"
        android:textAlignment="center"
        android:gravity="center"
        android:textColor="@color/white"/>

    <EditText
        android:id="@+id/loginEmail"
        android:layout_width="350dp"
        android:layout_height="50dp"
        android:layout_above="@+id/loginButton"
        android:layout_centerHorizontal="true"
        android:background="@drawable/white_background"
        android:ems="10"
        android:hint="@string/email_login_hint_text"
        android:layout_marginTop="215dp"
        android:textColorHint="#cbc4c4"
        android:inputType="textEmailAddress"
        android:paddingLeft="5dp"
        android:fontFamily="sans-serif"
        android:singleLine="true"
        android:drawableLeft="@drawable/email_icon">

        <requestFocus />
    </EditText>

    <EditText
        android:id="@+id/loginPassword"
        android:layout_width="250dp"
        android:layout_height="50dp"
        android:layout_alignLeft="@+id/loginEmail"
        android:layout_below="@+id/loginEmail"
        android:background="@drawable/password"
        android:paddingLeft="15dp"
        android:ems="10"
        android:hint="Password"
        android:textColorHint="#cbc4c4"
        android:password="true"
        android:fontFamily="sans-serif"
        android:singleLine="true"
        android:visibility="gone" />

    <Button
        android:id="@+id/loginButton"
        android:layout_width="350dp"
        android:layout_height="50dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="20dp"
        android:scaleType="fitXY"
        android:padding="0dp"
        android:background="@drawable/start_order_border"
        android:textAppearance="@style/FontSegoeUISemiLight"
        android:textColor="@color/white"
        android:textSize="20sp"
        android:text="Start Ordering, Om nom nom" />

    <ProgressBar
        android:id="@+id/progressBarMain"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/i_am_hungry"
        android:layout_centerHorizontal="true"
        android:layout_centerInParent="false"
        android:layout_marginTop="25dp"
        android:indeterminate="true"
        android:visibility="invisible" />
</RelativeLayout>